---
title: UnioAPI 蓝图贡献指南
description: 修改 UnioAPI 权威文档时必须遵守的流程与评审规则。
status: active
owner: 平台团队
last_updated: 2026-07-26
related:
  - README.md
  - docs/specifications/documentation.md
  - docs/specifications/coding.md
  - docs/templates/README.md
---

# 贡献指南

## 编写前检查

1. 搜索仓库，确认该概念是否已有权威位置。
2. 判断内容属于某个产品领域，还是平台级公共区域。
3. 明确这次变更属于提案、决策、规范还是实现细节。
4. 从 [文档模板](docs/templates/README.md) 中选择最接近的模板开始。

## 变更流程

1. 新建或更新唯一的权威文档。
2. 将其他位置的重复解释替换为指向该文档的链接。
3. 补齐规定的 YAML Front Matter。
4. 链接相关架构、规范、决策和产品文档。
5. 只有文档含义发生变化时才更新 `last_updated`。
6. 邀请文档负责人和受影响领域的负责人评审。

## 从实现仓库迁入资料

迁移前必须建立临时迁移计划、来源清单和目标映射。迁移以概念为单位，不以原文件为
单位：同一概念的多个来源应合并到一个权威位置，一份来源文件中的不同概念也可以拆分
到不同位置。迁移时按照[文档规范](docs/specifications/documentation.md)规定的资料优先级
解决冲突。当前行为必须以代码、Schema 和测试证明的实际逻辑为准；已批准目标和未实现优化分别记录，
不得用目标设计改写现状，也不得把当前代码行为仅因已经上线就自动视为永久产品或架构决策。

迁入内容先使用 `draft`；内容已经完整、可以交由负责人作出决定时使用 `proposed`。
只有目标文档的 `owner` 评审批准后，内容才能成为 `active`。迁入历史决策时，必须在
正文中保留原始 `DEC` 编号、日期和完整取代谱系；多个来源决策合并为一项当前决策时，
不得省略其中任何一个来源标识。

删除实现仓库中的来源文档前，必须同时满足以下条件：

- 每份来源均已记录目标位置或明确的“不迁移”处置；
- 决策编号、日期和取代谱系已保留；
- 仓库内外能够同步处理的链接已更新，不能同步的链接已有迁移说明；
- `make validate` 已通过；
- 蓝图文档负责人和受影响领域负责人已完成评审。

未满足门禁时，来源文档只能标记为待迁移，不能因目标草稿已经存在而提前删除。

## 实现变更交接

可能改变长期产品、架构或运行语义的代码工作，应先在实现仓库建立临时变更计划。计划
说明拟变更行为、影响范围、验证方式、回滚方式和可能受影响的蓝图文档。实现及测试完成
后，维护者应将已经证实的长期语义整理到蓝图，而不是复制实现步骤或测试日志；蓝图更新
按 `draft`、`proposed`、负责人批准为 `active` 的顺序评审。完成蓝图交接和校验后，临时
计划可按实现仓库规则删除或归档。

若变更不产生长期语义，例如只重构内部实现，或修复代码使其重新符合现有蓝图，变更计划
或评审记录必须明确写出“无需更新蓝图”及理由。测试通过本身既不代表蓝图已更新，也不
代表新行为已获得批准。详细工程基线见[编码规范](docs/specifications/coding.md)。

## 本地校验

提交评审前运行：

```sh
make validate
```

校验器不依赖第三方包，会检查目录结构、Front Matter、目录说明、命名规则和
本地 Markdown 链接。

## 评审清单

- 文档具有明确的负责人和生命周期状态。
- 内容应该属于蓝图，而不是某个代码实现仓库。
- 产品领域按产品命名，不按仓库命名。
- 术语遵循平台词汇表，或在首次使用前给出定义。
- 结论具体明确；需要验证的内容可验证，且没有隐藏假设。
- 相关内容通过链接引用，没有重复维护。
- 文件和目录名使用小写 kebab-case。

## 移动内容

权威位置发生变化时，必须在同一次变更中更新仓库内的引用。不要保留第二份继续
维护的副本。若外部链接无法同步更新，可以临时保留简短的迁移说明。
